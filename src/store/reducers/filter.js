
const initialState = {
   byCategory: '',
   byColors: '',
   bySize: {},
   byImageType: 'all',
   order: 'popular'

}

export default (state = initialState, action) => {

   switch(action.type) {

      default :
         return state
   }
}